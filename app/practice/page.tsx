'use client';
import Link from 'next/link';
import {useState} from 'react';
export default function Practice(){const [view,setView]=useState('home');const [saved,setSaved]=useState(false);return <main className="practice"><Link href="/" className="brand">once</Link><p className="muted" style={{margin:'12px 0 30px'}}>Capture practice · fictional workspace</p><h1>Invite a teammate</h1><p>Start the Once extension on this page. Click Workspace settings, then Members, then Send invitation.</p><button className="btn" onClick={()=>setView('settings')}>Workspace settings</button>{view!=='home'&&<button className="btn" onClick={()=>setView('members')}>Members</button>}{view==='members'&&<><label>Teammate name<input defaultValue="Alex Example"/></label><label>Email (fictional)<input type="email" defaultValue="alex@example.com"/></label><button className="btn primary" onClick={()=>setSaved(true)}>Send invitation</button>{saved&&<p role="status">Practice invitation created. No email was sent.</p>}</>}<p className="muted" style={{marginTop:30}}>Stop recording and open the editor to review your guide.</p></main>;}
